# CRUD App (LocalStorage)

Minimal React + TypeScript app that stores entities in LocalStorage. Intended for quick deploy to Vercel after `npm install`.

Quick start

```bash
cd crud-app
npm install
npm run dev
```

Features
- Create/Edit/Delete entities by type
- Search by `code` (choose type then search)
- LocalStorage persistence
